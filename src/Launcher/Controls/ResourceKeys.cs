using System.Windows;

namespace Launcher.Controls
{
    public static class ResourceKeys
    {
        public static readonly ComponentResourceKey HyperlinkButtonStyleKey = new(typeof(ResourceKeys), new());
        public static readonly ComponentResourceKey DeleteEntryButtonTemplateKey = new(typeof(ResourceKeys), new());
    }
}
